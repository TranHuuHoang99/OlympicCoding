#include <bits/stdc++.h>
#define ll long long

using namespace std;


void solve(void) {
	int a, b, c;
	cin >> a >> b >> c;

}

void rwFile(string input, string output) {
	freopen(input.c_str(), "r", stdin);
	freopen(output.c_str(), "w", stdout);
}

int32_t main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
	rwFile("AC1.inp", "AC1.out");
	int t;
	cin >> t;
	while (t--) {
		solve();
	}
	
    return 0;
}

